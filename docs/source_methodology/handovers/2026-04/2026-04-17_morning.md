# ═══════════════════════════════════════════════════════════════
# CTO HANDOVER — en1gma Kernel
# From: Outgoing CTO Claude (session 2026-03-31 → 2026-04-17)
# To: Incoming CTO Claude
# Date: 2026-04-17
# ═══════════════════════════════════════════════════════════════

# READ THIS FIRST. Then CLAUDE.md. Then FORWARD_PLAN.md.
# Those two docs will be updated by Opus before you start.
# This handover covers what happened, what's broken, and what's next.

# ═══════════════════════════════════════════════════════════════
# 1. WHAT HAPPENED THIS SESSION (2026-03-31)
# ═══════════════════════════════════════════════════════════════

session_deliveries:

  M3_DAILY_EXPANSION:
    status: SHIPPED
    what: |
      First Map-gated strategy YAML. Extracted hardcoded DMB config
      into strategies/daily_expansion.yaml. Built YAML loader.
      Replaced all hardcoded strategy refs in map_orchestrator.
    key_decisions:
      - "DMB renamed to DAILY_EXPANSION (family: DAILY_MOMENTUM)"
      - "strategy boundary = market state, NOT clock time (Olya decision)"
      - "cascade kill zone model SUPERSEDED by day-state engine"
      - "DAILY_CONTINUATION parked as second child (chain TBD from Olya)"
    tests: "142 passing at M3 ship"

  DAY_STATE_ENGINE:
    status: "SHIPPED but UNWIRED (SW03 — see sweep findings)"
    what: |
      2-state market-centric FSM: PRE_EXPANSION / POST_EXPANSION.
      Controls strategy eligibility. Resets at forex day boundary.
      Reads regime.direction from Map (read-only, never writes).
    invariant: INV-DAY-STATE-MARKET-DRIVEN
    critical_defect: |
      SW03: process_detections() implemented + tested (20 tests)
      but NEVER CALLED from map_orchestrator. Dead code in production.
      Literal 1-line fix to wire it. CRITICAL priority.

  M3_1_DR_CASCADE:
    status: SHIPPED
    what: |
      DailyRangeValidator detects when daily dealing range is still
      expanding (one-sided, no confirmed pullback swing). When invalid,
      system cascades to H4 dealing range for zone classification.
      Unlocked bullish expansion trades (4/6 ground truth were invisible).
    olya_calibration: |
      "I always respect premium/discount, but only relative to the
      ACTIVE dealing range. If daily is still expanding, I use H4."
      Answer C from calibration options — dealing range updates,
      not zone exemption.
    tests: "157 passing at M3.1 ship"

  DISCOVERY_SCAN:
    status: "RESULTS CONTAMINATED — must re-run after SW02 fix"
    progression:
      v1: {trades: 7, r: -1.00, longs: 0, bug: "static Map — frozen bearish regime"}
      v2: {trades: 6, r: +3.00, longs: 1, bug: "per-date Map fixed regime transitions"}
      v3: {trades: 22, r: +23.00, longs: 10, bug: "DR cascade fixed bullish blindness"}
    warning: |
      v3 numbers (+23R, 68% win rate) are UNRELIABLE.
      SW02 (future leak in replay) contaminates all results.
      After Block 0 fixes, re-run as v4 and compare.
      Architecture is correct — execution was contaminated.

# ═══════════════════════════════════════════════════════════════
# 2. WHAT HAPPENED DURING G's BREAK (Apr 1-16)
# ═══════════════════════════════════════════════════════════════

break_period:
  
  shipped_by_coo:
    sentinel: |
      Gemma 4 26B-A4B-4bit always-on system monitor on M3.
      5 health checks, ARS notification relay to Telegram.
      CODE COMPLETE but crontab NOT installed (macOS sandbox).
      G installing cron TODAY.
    henry: |
      GLM 5.1 offline forensic analyst on M3 Ultra.
      Methodology bundle system. Structured YAML findings.
      OPERATIONAL.
    ars_fixes: |
      Session date bug fixed (ea72990).
      ASIA_COMPLETE notification added.
  
  multi_model_sweep:
    date: "2026-04-17"
    auditors: "Claude Opus High + GPT-5.4 High"
    result: "22 new findings: 3 CRITICAL, 6 HIGH, 10 MEDIUM, 5 LOW"
    ref: "docs/CTO_BRIEF_2026-04-17.yaml (strategic) + docs/SWEEP_FINDINGS_2026-04-17.yaml (implementation)"

  olya_input: NONE (19 days, 10 items queued)
  tests: "202 passing (was 142 at M3 ship)"

# ═══════════════════════════════════════════════════════════════
# 3. WHAT'S BROKEN — PRIORITY ORDER
# ═══════════════════════════════════════════════════════════════

critical_defects:

  PHASE_0_TODAY:
    SW05:
      severity: "HIGH — PRODUCTION"
      what: "River burst bar drop — next_bar() skips bars on poll delay"
      affects: "LIVE ARS daemon on M3"
      fix: "queue-based next_bar() buffering"
      effort: "0.5 day"
      ref: "data/river.py:155-194"

    sentinel_cron:
      what: "G installs crontab manually from terminal"
      effort: "5 min"
      status: "G doing today"

  PHASE_1_BLOCK_0:
    label: "Unblock Map-gated path — each independently fatal"
    effort: "1-2 days"

    SW01:
      severity: CRITICAL
      what: "PDA mitigation direction INVERTED"
      detail: |
        Code checks expansion-direction penetration.
        ICT/vLOCK requires retrace-fills-through.
        In bullish: close > zone_top trivially true → all bullish
        FVGs immediately mitigate → zero active PDAs → gate never arms.
      fix: "swap mitigation conditions in pda_store.py + map_engine.py"
      blocker: "Olya confirmation (Telegram yes/no — G sending today)"
      ref: "context/pda_store.py:118-125, context/map_engine.py:390-396"

    SW02:
      severity: CRITICAL
      what: "Map replay loads ALL bars through end_date — future leak"
      detail: |
        run_map_replay() initializes MapEngine once from full dataset.
        LTF detections built once per KZ from full window.
        Forward-contaminated decisions. INVALIDATES walk-forward.
      fix: "per-date Map re-init with 45-day sliding window"
      note: "ALREADY BUILT for discovery scan v2 fix — same pattern"
      ref: "control/map_orchestrator.py:128-162, 248-306"

    SW03:
      severity: CRITICAL
      what: "DayStateEngine.process_detections() never called"
      detail: |
        Implemented, tested (20 tests), but not wired in orchestrator.
        FSM never transitions. DAILY_EXPANSION eligible all session.
      fix: "add process_detections() call in map_orchestrator"
      effort: "trivial — 1 function call"
      ref: "control/map_orchestrator.py"

  PHASE_2_BLOCK_1:
    label: "Harden for dual paper trading"
    effort: "2-3 days"
    items:
      SW08: "governance bypass — wire full check_governance() both paths"
      SW09: "ARS restart state loss — persist phase checkpoints"
      SW04: "PDA direction/zone corruption from detector"
      SW10: "pda_timeframes loaded but never enforced in gate"
      SW21: "5 missing scenario tests"

  PHASE_3_BLOCK_2:
    label: "Pre-live hardening"
    effort: "2-3 days"
    items: [SW06, SW07, SW11, SW12, SW13, SW14]

  PHASE_4_CLEANUP:
    items: [SW15, SW16, SW17, SW18, SW19, SW20, SW22]

# ═══════════════════════════════════════════════════════════════
# 4. OLYA QUEUE — 10 ITEMS
# ═══════════════════════════════════════════════════════════════

olya_queue:
  
  immediate_telegram:
    SW01: |
      "Does PDA mitigation mean retrace fills through zone
      (not expansion passing through)? e.g. bearish FVG mitigated
      when price retraces UP and closes above zone_top?"
      Expected answer: YES. Q14 already said "body close through far edge."
      Code does the OPPOSITE. Quick confirm unblocks Block 0.

  next_session:
    ARS_v1_4: "11 new trades for TradingView verification (+83% R improvement)"
    sweep_direction: "ruling on sweep direction convention for chain"
    F2_ote: "body-to-body or wick-to-wick for OTE measurement"

  can_wait: [F10, F2b, F4, F8, F9, vLOCK_cleanup]

# ═══════════════════════════════════════════════════════════════
# 5. LAB STATUS
# ═══════════════════════════════════════════════════════════════

lab:
  dexter:
    briefed: "5 chain variants (Olya-originated)"
    status: "HOLD — awaiting clean v4 scan (post-SW02 fix)"
    variants: [baseline, 15m_full_ote, 5m_full_ote, 15m_momentum, 5m_momentum]
    rule: "INV-NOTHING-CROSSES-WITHOUT-HUMAN"

  ars_v1_4:
    what: "body SL + 0.3pip buffer variant"
    result: "+83% total R, +12.7% win rate in backtest"
    status: "BLOCKED on Olya TradingView verification of 11 trades"

# ═══════════════════════════════════════════════════════════════
# 6. FORWARD PLAN — REVISED SEQUENCE
# ═══════════════════════════════════════════════════════════════

revised_milestones:

  COMPLETE:
    M1: "chain_evaluator (2026-03-30)"
    M2: "Map-to-chain wiring (2026-03-30)"
    M3: "DAILY_EXPANSION YAML + day-state (2026-03-31)"
    M3.1: "DR authority cascade daily→H4 (2026-03-31)"

  NEXT:
    M3.5_SWEEP_REMEDIATION:
      what: "Block 0 + Block 1 fixes from multi-model sweep"
      effort: "4-6 days engineering + 2-3 Olya sessions"
      gate: "all CRITICALs + HIGHs resolved, 202+ tests, no regressions"
      sub_phases:
        Phase_0: "SW05 production fix (today)"
        Phase_1: "SW01+SW02+SW03 Block 0 (1-2 days, needs Olya for SW01)"
        Phase_2: "SW08+SW09+SW04+SW10+SW21 Block 1 (2-3 days)"

    M4_WALK_FORWARD_REVISED:
      what: "walk-forward validation with CLEAN replay (post-SW02)"
      prerequisite: M3.5
      effort: "1-2 days + Olya validation"
      note: "ALL prior walk-forward results are VOID"

    DUAL_PAPER:
      what: "ARS + DAILY_EXPANSION simultaneously on M3"
      prerequisite: "M4 + Block 1 (governance wired)"

  PARKED:
    DAILY_CONTINUATION: "child strategy — chain/entry TBD from Olya"
    lab_variants: "5 chain configs — awaiting clean scan data"
    dream_cycle: "6+ months production data needed"

# ═══════════════════════════════════════════════════════════════
# 7. KEY INVARIANTS REGISTERED THIS SESSION
# ═══════════════════════════════════════════════════════════════

new_invariants:
  INV-DAY-STATE-MARKET-DRIVEN: |
    Day state transitions are triggered by detected market structure
    events (displacement + MSS), never by system execution events
    (intent creation, order placement, fills).

# ═══════════════════════════════════════════════════════════════
# 8. ARCHITECTURAL DECISIONS — REFERENCE
# ═══════════════════════════════════════════════════════════════

key_decisions_this_session:
  
  strategy_family_model: |
    DAILY_MOMENTUM family with children: DAILY_EXPANSION (shipped),
    DAILY_CONTINUATION (parked). Strategy boundary = market state.
    Session (LOKZ/NYOKZ) = execution parameter, not strategy boundary.
    Decided by Olya + advisor, confirmed by CTO + Map Advisor.

  cascade_superseded: |
    Original model: LOKZ-first, suppress NYOKZ on intent.
    Replaced by: day-state FSM. Both windows check day_state
    independently. No cascade logic needed.
    Reason: "state > timing" (Olya's model).

  dealing_range_authority: |
    When daily DR is still expanding (one-sided), cascade to H4.
    DailyRangeValidator detects: no pullback swing, price hasn't
    rotated around equilibrium, DR outrun (price beyond extreme).
    Gate uses active DR for zone classification — already DR-agnostic.
    Olya: "I always respect premium/discount relative to the ACTIVE range."

  yaml_thinness: |
    Strategy YAML declares WHAT (filter predicates), not HOW (Map mechanics).
    proximity_mode, zone_requirement, dealing_range_model are Map-level config,
    NOT per-strategy. "If there's no choice, don't expose a knob."
    Decided by Map Advisor review, confirmed by CTO.

# ═══════════════════════════════════════════════════════════════
# 9. DOCUMENTS TO READ
# ═══════════════════════════════════════════════════════════════

orientation_docs:
  primary:
    CLAUDE_md: "agent orientation — Opus updating to v0.7 NOW"
    FORWARD_PLAN_md: "roadmap — Opus updating to v1.3 NOW"
  
  sweep_reference:
    CTO_BRIEF: "docs/CTO_BRIEF_2026-04-17.yaml — strategic decisions"
    SWEEP_FINDINGS: "docs/SWEEP_FINDINGS_2026-04-17.yaml — implementation detail for Opus"
  
  methodology:
    calibration_results: "calibration_results.yaml — 27 Olya answers (LOCKED)"
    vLOCK: "SYNTHETIC_OLYA_METHOD_vLOCK.yaml — 13 L1 primitives"

  do_NOT_read:
    - "DAILY_MOMENTUM_BREAKOUT_CANON.md — SUPERSEDED by Map + YAML"
    - "old sprint briefs — everything is in the 4 canonical docs"

# ═══════════════════════════════════════════════════════════════
# 10. PARALLEL WORK STREAMS
# ═══════════════════════════════════════════════════════════════

parallel_now:
  opus_cursor: "doc updates (CLAUDE.md, FORWARD_PLAN) + SW05 fix"
  g: "sentinel cron install + Olya SW01 question"
  coo_m3: "available for production verification after SW05 deploys"
  dexter_lab: "HOLD — awaiting clean v4 scan"

# ═══════════════════════════════════════════════════════════════
# END HANDOVER
# ═══════════════════════════════════════════════════════════════
